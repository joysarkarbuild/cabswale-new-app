import 'package:flutter/material.dart';

class VideosFromRealDriversScreen extends StatelessWidget {
  const VideosFromRealDriversScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Videos from real drivers'),
      ),
    );
  }
}
